/**
 * Root component for the ported Ink/React TUI (Milestone 1).
 *
 * @license
 * Ported from Google gemini-cli (Apache-2.0). See NOTICE in repo root.
 */

import { AppContainer } from "./components/AppContainer.js";

export function App() {
  return <AppContainer />;
}
